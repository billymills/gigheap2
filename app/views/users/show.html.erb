<% provide(:title, @user.name) %>
<div class="container">
	<div class="row">
		<aside class="span4">
			<section>
				<h1>
					<%= gravatar_for @user %>
					<%= @user.name %>
					<%= @user.email  %>
				</h1>
			</section>
		</aside>
	</div>
</div>
